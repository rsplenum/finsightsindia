---
export interface Props {
	class?: string;
}

const { class: className = '' } = Astro.props;
---

<section class={`card-premium rounded-2xl reveal p-6 sm:p-8 space-y-6 transition-colors duration-200 ${className}`}>
	<slot />
</section>
